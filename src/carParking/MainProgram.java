package carParking;


public class MainProgram {
	//MVC format is a format of coding that seperates the view,
	//the model, and the controller, or in short, what is displayed, 
	//what information is transfered and how things are handled

	
	public static void main(String[] args) {
		//ParkingModel model = new ParkingModel();
		//ParkingView view = new ParkingView(model);
		//ParkingController controller = new ParkingController(model,view);
		
		//view.setVisible(true);
	}

}
